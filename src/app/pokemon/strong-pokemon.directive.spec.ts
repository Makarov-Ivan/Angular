import { StrongPokemonDirective } from './strong-pokemon.directive';

describe('StrongPokemonDirective', () => {
  it('should create an instance', () => {
    const directive = new StrongPokemonDirective();
    expect(directive).toBeTruthy();
  });
});
